/**
 * DEVICE-MANAGER.JS v4.0 - ZERO TOLERANCE
 * BLE-Geräteverwaltung mit Persistenz & Hardware-Integration
 */
'use strict';

const DEVICE_CONFIG = { STORAGE_KEY: 'led-devices', HISTORY_KEY: 'device-connection-history', GROUPS_KEY: 'device-groups', PROTOCOLS: [{ id: 'ELK_BLEDOM', name: 'ELK-BLEDOM (Standard)', description: 'Für ELK-BLEDOM LED-Streifen', commands: { on: [0x7E, 0x00, 0x04, 0x01, 0x00, 0x00, 0x00, 0xEF], off: [0x7E, 0x00, 0x04, 0x00, 0x00, 0x00, 0x00, 0xEF], color: function (r, g, b) { return [0x7E, 0x00, 0x05, r, g, b, 0x00, 0xEF]; }, brightness: function (val) { return [0x7E, 0x00, 0x0E, val, 0x00, 0x00, 0x00, 0xEF]; } } }, { id: 'GENERIC', name: 'Generic BLE LED', description: 'Für generische BLE-LED-Controller', commands: { on: [0x01, 0xFF], off: [0x01, 0x00], color: function (r, g, b) { return [0x02, r, g, b]; }, brightness: function (val) { return [0x03, val]; } } }], RSSI_EXCELLENT: -50, RSSI_GOOD: -70, RSSI_FAIR: -80, RSSI_POOR: -90, RECONNECT_TIMEOUT: 5000, MAX_RECONNECT_ATTEMPTS: 3 };

class DeviceManager {
    constructor() { this.devices = []; this.groups = []; this.connectionHistory = []; this.currentDevice = null; this.isScanning = false; this.init(); }
    init() { this.loadDevices(); this.loadGroups(); this.loadHistory(); // console.log('\u2705 Device Manager initialisiert'); }
    async scanForDevices() { if (!window.ledController) throw new Error('BLE-Controller nicht verfügbar'); if (this.isScanning) throw new Error('Scan läuft bereits'); try { this.isScanning = true; // console.log('\ud83d\udd0d Scanne nach BLE-Geräten...'); if (window.showGlobalNotification) window.showGlobalNotification('Suche nach Geräten...', 'info', 3000); const device = await window.ledController.scan(); if (device) { const existingDevice = this.getDeviceById(device.id); if (existingDevice) { // console.log('\u2705 Bekanntes Gerät gefunden:', existingDevice.name); if (window.showGlobalNotification) window.showGlobalNotification('Gerät gefunden: ' + existingDevice.name, 'success'); return existingDevice; } else { const newDevice = { id: device.id, name: device.name, originalName: device.name, mac: device.id, protocol: 'ELK_BLEDOM', autoConnect: false, group: null, favorite: false, rssi: null, lastConnected: null, connectionCount: 0, notes: '', createdAt: Date.now(), updatedAt: Date.now() }; this.addDevice(newDevice); // console.log('\u2705 Neues Gerät hinzugefügt:', newDevice.name); if (window.showGlobalNotification) window.showGlobalNotification('Neues Gerät: ' + newDevice.name, 'success'); return newDevice; } } } catch (err) { console.error('\u274c Scan fehlgeschlagen:', err); if (window.showGlobalNotification) { if (err.name === 'NotFoundError') window.showGlobalNotification('Keine Geräte gefunden', 'warning'); else window.showGlobalNotification('Scan fehlgeschlagen', 'error'); } throw err; } finally { this.isScanning = false; } }
    async connectToDevice(deviceId, protocol) { const device = this.getDeviceById(deviceId); if (!device) throw new Error('Gerät nicht gefunden'); if (!window.ledController) throw new Error('BLE-Controller nicht verfügbar'); try { // console.log('\ud83d\udd17 Verbinde mit', device.name); if (window.showGlobalNotification) window.showGlobalNotification('Verbinde mit ' + device.name + '...', 'info', 2000); const useProtocol = protocol || device.protocol || 'ELK_BLEDOM'; let success = false; if (navigator.bluetooth) { try { const bleDevice = await navigator.bluetooth.requestDevice({ filters: [{ name: device.name }], optionalServices: ['0000ffe0-0000-1000-8000-00805f9b34fb'] }); const server = await bleDevice.gatt.connect(); const service = await server.getPrimaryService('0000ffe0-0000-1000-8000-00805f9b34fb'); const characteristic = await service.getCharacteristic('0000ffe1-0000-1000-8000-00805f9b34fb'); window.ledDevice = { device: bleDevice, server: server, characteristic: characteristic, isConnected: true }; success = true; } catch (bleErr) { console.warn('BLE-Verbindung fehlgeschlagen:', bleErr); } } if (!success && window.ledController.connect) { success = await window.ledController.connect(device); } if (success) { this.currentDevice = device; device.lastConnected = Date.now(); device.connectionCount++; device.protocol = useProtocol; device.updatedAt = Date.now(); this.saveDevices(); this.addToHistory(device); // console.log('\u2705 Verbunden mit', device.name); if (window.showGlobalNotification) window.showGlobalNotification('Verbunden mit ' + device.name, 'success'); return true; } else { throw new Error('Verbindung fehlgeschlagen'); } } catch (err) { console.error('\u274c Verbindung fehlgeschlagen:', err); if (window.showGlobalNotification) window.showGlobalNotification('Verbindung fehlgeschlagen', 'error'); throw err; } }
    disconnectDevice() { if (this.currentDevice) { // console.log('\ud83d\udd0c Trenne Verbindung zu', this.currentDevice.name); if (window.ledDevice && window.ledDevice.server) { window.ledDevice.server.disconnect(); window.ledDevice = null; } this.currentDevice = null; if (window.showGlobalNotification) window.showGlobalNotification('Verbindung getrennt', 'info'); } }
    addDevice(device) { if (!this.getDeviceById(device.id)) { this.devices.push(device); this.saveDevices(); // console.log('\u2705 Gerät hinzugefügt:', device.name); } }
    updateDevice(deviceId, updates) { const device = this.getDeviceById(deviceId); if (device) { Object.assign(device, updates); device.updatedAt = Date.now(); this.saveDevices(); // console.log('\u2705 Gerät aktualisiert:', device.name); return true; } return false; }
    deleteDevice(deviceId) { const index = this.devices.findIndex(function (d) { return d.id === deviceId; }); if (index !== -1) { const device = this.devices[index]; this.devices.splice(index, 1); this.saveDevices(); // console.log('\u2705 Gerät gelöscht:', device.name); return true; } return false; }
    getDeviceById(id) { return this.devices.find(function (d) { return d.id === id; }); }
    getAllDevices() { return this.devices; }
    getFavoriteDevices() { return this.devices.filter(function (d) { return d.favorite; }); }
    getDevicesByGroup(groupId) { return this.devices.filter(function (d) { return d.group === groupId; }); }
    toggleFavorite(deviceId) { const device = this.getDeviceById(deviceId); if (device) { device.favorite = !device.favorite; device.updatedAt = Date.now(); this.saveDevices(); // console.log(device.favorite ? '\u2b50 Favorit hinzugefügt' : '\u274c Favorit entfernt', device.name); return device.favorite; } return false; }
    setAutoConnect(deviceId, enabled) { const device = this.getDeviceById(deviceId); if (device) { device.autoConnect = enabled; device.updatedAt = Date.now(); this.saveDevices(); // console.log(enabled ? '\u2705 Auto-Connect aktiviert' : '\u274c Auto-Connect deaktiviert', device.name); return true; } return false; }
    renameDevice(deviceId, newName) { const device = this.getDeviceById(deviceId); if (device) { device.name = newName; device.updatedAt = Date.now(); this.saveDevices(); // console.log('\u2705 Gerät umbenannt zu:', newName); return true; } return false; }
    setDeviceProtocol(deviceId, protocolId) { const device = this.getDeviceById(deviceId); const protocol = DEVICE_CONFIG.PROTOCOLS.find(function (p) { return p.id === protocolId; }); if (device && protocol) { device.protocol = protocolId; device.updatedAt = Date.now(); this.saveDevices(); // console.log('\u2705 Protokoll gesetzt zu:', protocol.name); return true; } return false; }
    setDeviceGroup(deviceId, groupId) { const device = this.getDeviceById(deviceId); if (device) { device.group = groupId; device.updatedAt = Date.now(); this.saveDevices(); // console.log('\u2705 Gruppe gesetzt'); return true; } return false; }
    addToHistory(device) { const historyEntry = { deviceId: device.id, deviceName: device.name, timestamp: Date.now(), protocol: device.protocol }; this.connectionHistory.unshift(historyEntry); if (this.connectionHistory.length > 50) this.connectionHistory = this.connectionHistory.slice(0, 50); this.saveHistory(); }
    getConnectionHistory() { return this.connectionHistory; }
    clearHistory() { this.connectionHistory = []; this.saveHistory(); // console.log('\u2705 Verbindungs-Historie geleert'); }
    createGroup(name, color) { const group = { id: 'group_' + Date.now(), name: name, color: color || '#4ecdc4', deviceIds: [], createdAt: Date.now() }; this.groups.push(group); this.saveGroups(); // console.log('\u2705 Gruppe erstellt:', name); return group; }
    deleteGroup(groupId) { const index = this.groups.findIndex(function (g) { return g.id === groupId; }); if (index !== -1) { this.devices.forEach(function (d) { if (d.group === groupId) d.group = null; }); this.groups.splice(index, 1); this.saveDevices(); this.saveGroups(); // console.log('\u2705 Gruppe gelöscht'); return true; } return false; }
    getAllGroups() { return this.groups; }
    getGroupById(id) { return this.groups.find(function (g) { return g.id === id; }); }
    getProtocolById(id) { return DEVICE_CONFIG.PROTOCOLS.find(function (p) { return p.id === id; }); }
    getAllProtocols() { return DEVICE_CONFIG.PROTOCOLS; }
    getSignalStrength(rssi) { if (!rssi) return 'unknown'; if (rssi >= DEVICE_CONFIG.RSSI_EXCELLENT) return 'excellent'; if (rssi >= DEVICE_CONFIG.RSSI_GOOD) return 'good'; if (rssi >= DEVICE_CONFIG.RSSI_FAIR) return 'fair'; if (rssi >= DEVICE_CONFIG.RSSI_POOR) return 'poor'; return 'very-poor'; }
    loadDevices() { try { const stored = localStorage.getItem(DEVICE_CONFIG.STORAGE_KEY); if (stored) { this.devices = JSON.parse(stored); /* // console.log('✅ ' + this.devices.length + ' Geräte geladen'); */ } } catch (err) { console.error('Fehler beim Laden der Geräte:', err); this.devices = []; } }
    saveDevices() { try { localStorage.setItem(DEVICE_CONFIG.STORAGE_KEY, JSON.stringify(this.devices)); } catch (err) { console.error('Fehler beim Speichern der Geräte:', err); } }
    loadHistory() { try { const stored = localStorage.getItem(DEVICE_CONFIG.HISTORY_KEY); if (stored) { this.connectionHistory = JSON.parse(stored); } } catch (err) { console.error('Fehler beim Laden der Historie:', err); this.connectionHistory = []; } }
    saveHistory() { try { localStorage.setItem(DEVICE_CONFIG.HISTORY_KEY, JSON.stringify(this.connectionHistory)); } catch (err) { console.error('Fehler beim Speichern der Historie:', err); } }
    loadGroups() { try { const stored = localStorage.getItem(DEVICE_CONFIG.GROUPS_KEY); if (stored) { this.groups = JSON.parse(stored); } } catch (err) { console.error('Fehler beim Laden der Gruppen:', err); this.groups = []; } }
    saveGroups() { try { localStorage.setItem(DEVICE_CONFIG.GROUPS_KEY, JSON.stringify(this.groups)); } catch (err) { console.error('Fehler beim Speichern der Gruppen:', err); } }
    exportDevices() { return JSON.stringify({ devices: this.devices, groups: this.groups, history: this.connectionHistory, exportedAt: Date.now() }, null, 2); }
    importDevices(jsonString) { try { const data = JSON.parse(jsonString); if (data.devices) this.devices = data.devices; if (data.groups) this.groups = data.groups; if (data.history) this.connectionHistory = data.history; this.saveDevices(); this.saveGroups(); this.saveHistory(); // console.log('\u2705 Geräte importiert'); return true; } catch (err) { console.error('Import-Fehler:', err); return false; } }
}

window.DeviceManager = DeviceManager;
window.deviceManager = new DeviceManager();
// console.log('\u2705 Device Manager global verfügbar als window.deviceManager');

if (typeof module !== 'undefined' && module.exports) module.exports = { DeviceManager, DEVICE_CONFIG };
