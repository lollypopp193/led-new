/**
 * AUDIO-REACTIVE-ENGINE.JS v4.0 - ZERO TOLERANCE
 * Vollständige Audio-Analyse: BeatDetection, BPM, Multi-Band, Hardware-Integration
 */
'use strict';

class BeatDetector {
    constructor() { this.previousLevels = []; this.beatThreshold = 1.3; this.beatHoldFrames = 20; this.framesSinceLastBeat = 0; this.beatDetected = false; this.beatCallbacks = []; this.beatHistory = []; this.maxHistoryLength = 100; }
    analyze(dataArray) { if (!dataArray || dataArray.length === 0) return false; let sum = 0; for (let i = 0; i < dataArray.length; i++) sum += dataArray[i]; const avg = sum / dataArray.length; this.previousLevels.push(avg); if (this.previousLevels.length > 60) this.previousLevels.shift(); const histAvg = this.previousLevels.reduce(function (a, b) { return a + b; }, 0) / this.previousLevels.length; this.framesSinceLastBeat++; if (avg > histAvg * this.beatThreshold && this.framesSinceLastBeat > this.beatHoldFrames) { this.beatDetected = true; this.framesSinceLastBeat = 0; const now = Date.now(); this.beatHistory.push(now); if (this.beatHistory.length > this.maxHistoryLength) this.beatHistory.shift(); this.beatCallbacks.forEach(function (cb) { cb(); }); return true; } this.beatDetected = false; return false; }
    onBeat(callback) { this.beatCallbacks.push(callback); }
    getBPM() { if (this.beatHistory.length < 2) return 0; const recent = this.beatHistory.slice(-10); if (recent.length < 2) return 0; let totalInterval = 0; for (let i = 1; i < recent.length; i++) totalInterval += recent[i] - recent[i - 1]; const avgInterval = totalInterval / (recent.length - 1); const bpm = Math.round(60000 / avgInterval); return bpm > 0 && bpm < 300 ? bpm : 0; }
    getLatency() { return this.framesSinceLastBeat * 16; }
    reset() { this.previousLevels = []; this.framesSinceLastBeat = 0; this.beatDetected = false; this.beatHistory = []; }
}

class AudioReactiveEngine {
    constructor() { this.audioContext = null; this.analyser = null; this.source = null; this.microphone = null; this.dataArray = null; this.isRunning = false; this.animationFrame = null; this.beatDetector = new BeatDetector(); this.cleanupFunctions = []; this.musicPlayerConnected = false; this.audioSourceType = null; this.ledStrips = []; this.fftSize = 2048; this.bassRange = { start: 0, end: 15 }; this.midRange = { start: 15, end: 150 }; this.trebleRange = { start: 150, end: 512 }; this.vocalRange = { start: 80, end: 250 }; this.useBandSettings = true; this.genreHistory = []; this.currentGenre = 'unknown'; this.lastIntensity = 0; this.intensitySmoothing = 0.7; this.flickerThreshold = 0.3; this.init(); }
    init() { this.createFrequencyBars(); if (typeof window.bandSettings !== 'undefined') this.syncWithBandSettings(); this.setupEventListeners(); this.setupMusicPlayerIntegration(); console.log('\u2705 Audio-Reactive Engine initialisiert'); }
    async startAudioCapture(audioElement) { try { await this.stopAudioCapture(); this.audioContext = new (window.AudioContext || window.webkitAudioContext)(); if (audioElement && audioElement.tagName === 'AUDIO') { console.log('\ud83c\udfb5 Audio-Element Analyse'); this.source = this.audioContext.createMediaElementSource(audioElement); this.audioSourceType = 'music-player'; } else { console.log('\ud83c\udfa4 Mikrofon Analyse'); const stream = await navigator.mediaDevices.getUserMedia({ audio: true }); this.microphone = stream; this.source = this.audioContext.createMediaStreamSource(stream); this.audioSourceType = 'microphone'; } this.analyser = this.audioContext.createAnalyser(); this.analyser.fftSize = 256; this.analyser.smoothingTimeConstant = 0.8; this.source.connect(this.analyser); this.source.connect(this.audioContext.destination); this.dataArray = new Uint8Array(this.analyser.frequencyBinCount); this.isRunning = true; this.processAudio(); return true; } catch (e) { console.error('\u274c Audio-Capture:', e); await this.stopAudioCapture(); return false; } }
    async stopAudioCapture() { this.isRunning = false; if (this.animationFrame) { cancelAnimationFrame(this.animationFrame); this.animationFrame = null; } this.cleanupFunctions.forEach(function (fn) { try { fn(); } catch (e) { } }); this.cleanupFunctions = []; if (this.audioContext && this.audioContext.state !== 'closed') { try { await this.audioContext.close(); } catch (e) { } } this.audioContext = null; this.analyser = null; this.microphone = null; this.dataArray = null; }
    processAudio() { if (!this.isRunning) return; if (!this.dataArray) this.dataArray = new Uint8Array(this.analyser.frequencyBinCount); this.analyser.getByteFrequencyData(this.dataArray); const bass = this.getFrequencyRangeLevel(this.bassRange); const mid = this.getFrequencyRangeLevel(this.midRange); const treble = this.getFrequencyRangeLevel(this.trebleRange); const beat = this.beatDetector.analyze(this.dataArray); this.updateFrequencyBars(); this.updateStatusDisplay(bass, mid, treble, beat); this.updateLEDStrips(bass, mid, treble, beat); this.animationFrame = requestAnimationFrame(function () { this.processAudio(); }.bind(this)); }
    getFrequencyRangeLevel(range) { let sum = 0; for (let i = range.start; i < range.end; i++) sum += this.dataArray[i]; return (sum / (range.end - range.start)) / 255; }
    updateFrequencyBars() { const bars = document.querySelectorAll('.freq-bar'); if (!bars || bars.length === 0) return; const step = Math.floor(this.dataArray.length / bars.length); bars.forEach(function (bar, idx) { const val = this.dataArray[idx * step]; bar.style.height = ((val / 255) * 100) + '%'; }.bind(this)); }
    updateStatusDisplay(bass, mid, treble, beat) { const elems = { bass: 'ledBassLevel', mid: 'ledMidLevel', treble: 'ledTrebleLevel', bpm: 'ledBPMValue', latency: 'ledLatencyValue' }; if (document.getElementById(elems.bass)) { document.getElementById(elems.bass).textContent = Math.round(bass * 100) + '%'; document.getElementById(elems.mid).textContent = Math.round(mid * 100) + '%'; document.getElementById(elems.treble).textContent = Math.round(treble * 100) + '%'; document.getElementById(elems.bpm).textContent = this.beatDetector.getBPM(); document.getElementById(elems.latency).textContent = this.beatDetector.getLatency() + 'ms'; const beatInd = document.getElementById('ledBeatIndicator'); if (beat && beatInd) { beatInd.classList.add('pulse'); setTimeout(function () { beatInd.classList.remove('pulse'); }, 100); } } }
    updateLEDStrips(bass, mid, treble, beat) { const vocal = this.getFrequencyRangeLevel(this.vocalRange); const genre = this.detectGenre(bass, mid, treble, vocal); this.ledStrips.forEach(function (strip, idx) { if (!strip.enabled) return; let intensity = 0; switch (strip.reactTo) { case 'rhythm': intensity = beat ? 1.0 : bass * 0.5; break; case 'beats': intensity = beat ? 1.0 : 0.1; break; case 'vocals': intensity = vocal; break; case 'melody': intensity = (mid * 0.7 + treble * 0.3); break; case 'gesang': intensity = vocal * 1.2; break; default: switch (strip.frequencyBand) { case 'bass': intensity = bass; break; case 'mid': intensity = mid; break; case 'high': case 'treble': intensity = treble; break; case 'all': intensity = (bass + mid + treble) / 3; break; } } intensity = intensity * (strip.sensitivity / 100) * (strip.brightness / 100); intensity = this.smoothIntensity(intensity); if (window.ledDevice && window.ledDevice.isConnected && window.ledDevice.characteristic) { try { const colorVal = Math.round(255 * intensity); let r = 0, g = 0, b = 0; if (genre === 'electronic') { r = colorVal; b = colorVal * 0.5; } else if (genre === 'rock') { r = colorVal; } else if (genre === 'jazz') { g = colorVal; b = colorVal * 0.7; } else { switch (strip.frequencyBand) { case 'bass': r = colorVal; break; case 'mid': g = colorVal; break; case 'high': case 'treble': b = colorVal; break; default: r = g = b = colorVal; } } if (beat && intensity > 0.5) r = g = b = 255; const cmd = new Uint8Array([0x7E, 0x00, 0x05, r, g, b, 0x00, 0xEF]); window.ledDevice.characteristic.writeValue(cmd); } catch (e) { } } }.bind(this)); }
    detectGenre(bass, mid, treble, vocal) { const bassRatio = bass / (mid + treble + 0.01); const trebleRatio = treble / (bass + mid + 0.01); const vocalRatio = vocal / (bass + mid + treble + 0.01); let genre = 'unknown'; if (bassRatio > 1.5 && trebleRatio > 1.2) genre = 'electronic'; else if (bassRatio > 1.3 && vocalRatio < 0.3) genre = 'rock'; else if (vocalRatio > 0.4 && mid > 0.5) genre = 'pop'; else if (trebleRatio > 1.1 && bassRatio < 0.8) genre = 'jazz'; this.genreHistory.push(genre); if (this.genreHistory.length > 30) this.genreHistory.shift(); const counts = {}; this.genreHistory.forEach(g => counts[g] = (counts[g] || 0) + 1); let maxGenre = 'unknown'; let maxCount = 0; Object.keys(counts).forEach(g => { if (counts[g] > maxCount) { maxCount = counts[g]; maxGenre = g; } }); this.currentGenre = maxGenre; return maxGenre; }
    smoothIntensity(intensity) { if (Math.abs(intensity - this.lastIntensity) > this.flickerThreshold) { intensity = this.lastIntensity * this.intensitySmoothing + intensity * (1 - this.intensitySmoothing); } this.lastIntensity = intensity; return intensity; }
    createFrequencyBars() { const cont = document.getElementById('ledFrequencyDisplay'); if (cont) { cont.innerHTML = ''; for (let i = 0; i < 64; i++) { const bar = document.createElement('div'); bar.className = 'freq-bar'; bar.style.cssText = 'height: 0%; flex: 1; background: linear-gradient(to top, #4ecdc4, #44a08d); border-radius: 2px; margin: 0 1px;'; cont.appendChild(bar); } } }
    syncWithBandSettings() { if (typeof window.bandSettings === 'undefined' || typeof window.activeBandCount === 'undefined') return; this.ledStrips = []; for (let i = 0; i < window.activeBandCount; i++) { const s = window.bandSettings[i]; this.ledStrips.push({ id: 'band_' + i, name: 'LED-Band ' + (i + 1), connected: false, enabled: s.enabled, frequencyBand: s.freqRange, effect: s.effect, sensitivity: s.sensitivity, color: s.color, brightness: s.brightness, speed: s.speed, reactTo: s.reactTo }); } this.renderAllStripControls(); }
    renderAllStripControls() { const cont = document.getElementById('ledStripsGrid'); if (!cont) return; cont.innerHTML = ''; this.ledStrips.forEach(function (strip) { const div = document.createElement('div'); div.className = 'strip-control'; div.style.cssText = 'background: rgba(255,255,255,0.05); padding: 15px; border-radius: 10px; margin-bottom: 10px;'; div.innerHTML = '<div style="font-weight: bold; color: #4ecdc4; margin-bottom: 10px;">' + strip.name + '</div><div style="font-size: 0.9rem; color: #ccc;">Frequenz: ' + strip.frequencyBand + '</div><div style="font-size: 0.9rem; color: #ccc;">Sensitivity: ' + strip.sensitivity + '%</div>'; cont.appendChild(div); }); }
    setupEventListeners() { const scanBtn = document.getElementById('scanLEDStrips'); if (scanBtn) scanBtn.addEventListener('click', function () { this.scanAndConnectDevices(); }.bind(this)); window.addEventListener('bandSettingsChanged', function () { this.syncWithBandSettings(); }.bind(this)); }
    setupMusicPlayerIntegration() { window.addEventListener('message', function (e) { const allowed = ['http://localhost', 'https://localhost', window.location.origin]; if (!allowed.some(function (o) { return e.origin.startsWith(o); })) return; if (e.data.type === 'musicPlayerAudioData') this.processMusicPlayerData(e.data.audioData); else if (e.data.type === 'musicPlayerConnected') this.musicPlayerConnected = true; else if (e.data.type === 'musicPlayerDisconnected') { this.musicPlayerConnected = false; if (this.audioSourceType === 'music-player') this.stopAudioCapture(); } }.bind(this)); if (window.parent && window.parent !== window) { window.parent.postMessage({ type: 'ledMusicControlReady' }, window.location.origin); } }
    async scanAndConnectDevices() { if (typeof window.bleController !== 'undefined' && window.bleController) { try { await window.bleController.scanForDevices(); const devices = window.bleController.getAvailableDevices(); devices.forEach(function (d, i) { if (this.ledStrips[i]) { this.ledStrips[i].connected = true; this.ledStrips[i].deviceInfo = d; } }.bind(this)); this.renderAllStripControls(); console.log('\u2705 ' + devices.length + ' Ger\u00e4te gefunden'); } catch (e) { console.error('\u274c BLE-Scan:', e); } } else { this.ledStrips.forEach(function (s) { s.connected = true; }); this.renderAllStripControls(); console.log('\u2705 LED-B\u00e4nder simuliert verbunden'); } }
    async startFileAnalysis(file) { try { console.log('\ud83c\udfb5 Starte Datei-Analyse mit AudioDecoderFFT'); if (!window.AudioDecoderFFT) { console.error('\u274c AudioDecoderFFT nicht verf\u00fcgbar'); return false; } await this.stopAudioCapture(); const decoder = new window.AudioDecoderFFT(); await decoder.init(); const success = await decoder.loadAudioFile(file); if (!success) return false; this.audioSourceType = 'file'; this.isRunning = true; decoder.startAnalysis(function (features) { this.updateStatusDisplay(features.bass, features.mid, features.treble, features.beat); this.updateLEDStrips(features.bass, features.mid, features.treble, features.beat); }.bind(this)); console.log('\u2705 Datei-Analyse gestartet'); return true; } catch (e) { console.error('\u274c Datei-Analyse Fehler:', e); return false; } }
}

window.BeatDetector = BeatDetector;
window.AudioReactiveEngine = AudioReactiveEngine;
window.audioReactiveEngine = new AudioReactiveEngine();
console.log('\u2705 Audio-Reactive Engine global verf\u00fcgbar als window.audioReactiveEngine');

if (typeof module !== 'undefined' && module.exports) module.exports = { BeatDetector, AudioReactiveEngine };
